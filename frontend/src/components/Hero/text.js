import { FormattedMessage } from "react-intl";

export const TitleText = (
  <FormattedMessage
      id="heroHeading"
      defaultMessage="{strong}"
    values={{ strong: <strong>Data Brokers Database</strong> }}
  />
);
